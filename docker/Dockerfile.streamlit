FROM python:3.12-slim
WORKDIR /app
RUN pip install --no-cache-dir streamlit plotly pandas numpy pyarrow
EXPOSE 8501
CMD ["streamlit", "run", "src/app.py", "--server.port=8501", "--server.address=0.0.0.0"]
